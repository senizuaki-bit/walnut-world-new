# 提交链路重复检查精简

## 已完成

1. 连续失败计数保留 Run、执行证据和终态命令检查，取消对每条历史 Run 的 Agent 回复、学习投影的递归重验。
2. 新结果按真实 Run 与失败后缀计算后直接保存，取消立即再计算一遍的校验。恢复及保存最终回复时，绑定已保存的结果与摘要，不再重新统计历史失败。恢复仍按当前 Run 与已保存计数重建事件字段，兼容既有普通 JSON 回执。
3. 提交后的 Teaching/Bug/Book 上下文使用 `FINAL_CONTEXT_READY` 保存一次，模型后续交互和 Worker 恢复直接复用。复用了已有 Hint 上下文保存逻辑，没有新增外部缓存或数据库表。

改动位于 `run_outcomes.py`、`turn_worker.py`，并补充计数及恢复测试。当前技能版本、真实运行结果、证据和重复提交保护仍保留。

## 真实验证

独立数据库副本、实际 Godot 提交流程、真实 DS 和 Docker，连续提交三个不同技能版本，错误计数为 1、2、3；回复依次为 Teaching、Teaching、Bug，全部 `source=provider` 且未降级，后台与学习投影均为 SUCCEEDED。Godot 完整跑完三轮，退出码为 0；退出阶段有资源未释放提示，未改变三轮完成断言。

| 轮次 | 提交至游戏收到回复 | Worker 活跃处理 | Worker SQL | Worker 轮数 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 54 秒 | 23.891 秒 | 10,309 | 5 |
| 2 | 58 秒 | 25.140 秒 | 10,963 | 5 |
| 3 | 64 秒 | 28.234 秒 | 13,417 | 5 |

第三轮旧样本的 Worker 为 182.234 秒、59,627 SQL、6 轮；新样本 SQL 减少约 77.5%。Bug 上下文由 4 次、65.517 秒、22,652 SQL，变为 1 次、4.875 秒、2,633 SQL。

旧样本前两轮收到回复为 96、124 秒，第三轮回复记录保存约 219 秒、后台成功约 231 秒，原游戏脚本总时限到达后通过恢复会话读取；因此旧第三轮没有可直接等同于新样本的连续客户端接收时间。

模型输出不是完全确定的：旧第三轮五次真实请求合计 6.25 秒，新第三轮四次请求合计 7.75 秒。上述是各一次真实样本，不是多轮统计；不能把全部时间差只归因于检查移除。重复上下文只构建一次、SQL 减少则有直接日志证据。

本次按要求先清理重复检查。模型 PENDING 仍通过 Worker 恢复，学习投影仍参与最终完成，没有宣称整个流程已经合并或全部性能问题已解决。

## 测试

- 后端单元、Hint 集成、Teaching→Teaching→Bug→Book、模型恢复与沙箱不重复执行：412 passed，1 条已有依赖警告。
- 新增真实 PostgreSQL 回执恢复测试及运行异常转换测试：2 passed。恢复测试使用新建的处理器两次读取同一回执，禁止调用历史计数函数。
- 最终计数边界、Agent 上下文和路由定向回归：46 passed、5 subtests passed。
- Ruff 与改动文件的空白检查通过。以上测试集合有重叠，不相加作为独立测试总数。

## 日志

- `bug-checks-simplified-validation.json`：三轮 Run、版本、角色、计数和耗时。
- `bug-checks-simplified-ui.jsonl`、`bug-checks-simplified-godot.log`：客户端真实结果。
- `bug-checks-simplified-*.stdout.log` / `*.stderr.log`：后台阶段日志。
- `validate_simplified_checks.py`：本次验证脚本。

原数据库没有这三条测试 Run；临时数据库与测试服务已在验证后清理，测试前处于停止状态的 PostgreSQL 容器恢复停止状态。
