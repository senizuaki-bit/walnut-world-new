# 双素材包复核与分支合并

日期：2026-09-08。当前开发目录：`D:/FeishuAIreview/walnut-world-new`，分支：`fronted-art`。

## 结论

作物适配浇水器学生端的运行时美术引用已统一到用户交付的 V2 静态包与 V1 动态包。两个分支的布局、动画和交互修复已合并；本次仅本地提交，无推送。可打开 [21页实机对照](two-pack-review.html)。

“全部来自素材包”的核对对象是本关美术资源，包括隐藏节点的资源依赖、动态加载的素材目录。原生文字、代码、字体及其他历史关卡不属于这两个美术交付包；本报告不声称整个仓库的文件都来自素材包。

## 原始依据

- `D:/WeChat Files/xwechat_files/wxid_go81hts4yiiu22_4547/msg/file/2026-09/动态素材包_V1/`：替换文件名列表、动画清单、逐帧元数据。
- `D:/WeChat Files/xwechat_files/wxid_go81hts4yiiu22_4547/msg/file/2026-09/作物适配浇水器_美术资源包_V2/`：完整对接指南、流程与美术契约、资源接入清单、技能树说明、asset-manifest、guide-layout、skill-tree-data。

包内“本次没有修改Godot”等描述是交付包的历史范围说明；浏览器模拟结果也不作为正式游戏判定。实际开发操作遵循用户本次的合并、修改及禁止推送要求。

## 来源核对

| 对象 | 结果 |
|---|---|
| V2可用静态资源 | 100/100，与原包逐文件SHA-256一致 |
| V2停用高亮框 | 3件均未接入；继续使用土地轮廓反馈 |
| V1动画 | 98/98，元数据、poster、图集或OGV文件齐全，帧时长和图集边界通过 |
| V2参考状态 | 74/74，全部资源ID有效，未引用已停用图 |
| 对照原文件 | 398项比对通过，0差异 |
| 素材目录全部媒体 | 302个文件均能在两个原包中找到相同SHA-256 |
| 首页与本关传递依赖 | 59个字面量媒体引用全部来自两包；动态格式化路径通过完整素材目录核对 |

已清除新分支引入的两张非交付背景及自制纸框、技能连线等11个过时文件，并替换首页、关卡和对话场景中的旧美术依赖。历史验收截图继续作为文档存在，不进入运行时美术。

完整记录：[来源、98段动画触发说明与74态清单](verification/two-pack-source-audit.json)。校验器直接读取用户原文件，不信任项目旧的校验清单：

```powershell
python scripts/verify-art-source-packs.py --motion-pack '动态素材包_V1的绝对路径' --static-pack '作物适配浇水器_美术资源包_V2的绝对路径'
```

## 接入要求与修正

| 要求 | 实现与证据 |
|---|---|
| 原型不能整页盖在交互上 | 保留V2预置控件布局、真实Button/LineEdit/CodeEdit；使用独立组件 |
| 动画不能被静态布局覆盖 | 合回环境、角色说话/待机/读数、作物、水泵与工坊反馈；旧分支只启用水流的退化已修复 |
| 状态不改变世界判定 | 候选湿度仍为0–10000，作物表现消费evaluator状态，不写入权威湿度；不足结果不再保留上次达标皮肤 |
| 浇水与暂停恢复 | 1份/2份使用原图集约2秒/4秒、单次播放；保留候选速度、暂停、跳过、回放的现有语义 |
| 土地反馈遵循透明轮廓 | 使用对应soil状态的V1轮廓图集；旧矩形框隐藏，土地和命中区域不缩放 |
| 输入反馈与模态 | 保留独立字段校验、错误说明、焦点恢复和键盘拦截；修复合并产生的重复_input和滚动节点路径 |
| E03/E10技能树 | 五个图标、三项原始说明、按状态显示完成勾；修正完成勾越界，未来节点保持禁用 |
| E07同一件浇水器 | 补回icon-skill-watering及说明，按reward.toolRect/bodyRect放置；三个按钮保持原信号 |
| Patch接受/拒绝/关闭 | 保留预置双栏及三个操作，接受仅生成草稿，仍需学生再次运行 |
| 加载与隐藏 | 按当前可见状态加载图集；隐藏动画停止推进，保留poster回退；未恢复用户已移除的“减少动态”开关 |

## 验证证据与边界

- Godot 4.7.1，完整离线回归 **80/80通过**：[结果](verification/two-pack-tests.json)。
- 最终技能树说明与标记修正后，原型契约测试和动画交互专项测试再次通过。
- 21个页面重新进行Godot图形渲染截图，无脚本/资源错误：[记录](verification/two-pack-captures.json)。农田、工坊、技能树和获得工具页另有人工图像检查。
- 74态是参考资源与映射核对数，不是74次真实线上业务验收。现有业务测试覆盖输入、保存冲突、失败、候选与提案等分支；网页人为设置的checking/success等状态不能据此伪造正式Run、Receipt或解锁。
- 98段是完整交付文件验证数；运行时按状态选用，并非98段同时播放，也不把未被当前流程触发的备选片段标成已实机验收。
- 本轮未访问真实网关写接口、修改用户存档或进行线上端到端验收。

## 分支结果

- `e53e74b`：合并提交，保留原`fronted-art`与额外分支两边的开发历史和功能。
- 本地`codex/bug-legion-production-chain`已删除。
- `D:/FeishuAIreview/walnut-world-art-v2`保留原文件，处于原提交`11c7366`的detached HEAD；当前开发以`walnut-world-new`为准。
- 远程`fronted-art`与额外分支均未改动。删除远程分支属于远程写操作，本轮按“不要推送”未执行。

本记录取代旧`art-v2-implementation.md`中“仅接入V1浇水、79项测试”的当前状态描述；旧原型记录仅保留历史意义。
