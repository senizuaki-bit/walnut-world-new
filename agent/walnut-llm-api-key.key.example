# Walnut LLM API Key 文件模板
#
# 把本文件复制为 `walnut-llm-api-key.key`，然后将你自己的 DeepSeek
# (或其它 LLM provider) API key 粘贴进去（不带换行、不带引号）。
#
#   cp walnut-llm-api-key.key.example walnut-llm-api-key.key
#   # 编辑 walnut-llm-api-key.key，粘贴你的 key
#
# 后端通过环境变量读取该文件：
#   WALNUT_LLM_UPSTREAM_API_KEY_FILE=<此文件的绝对路径>
#
# 安全提示：
# - 真实的 `*.key` 已被 .gitignore 忽略，绝不会提交到 GitHub。
# - 不要把 key 直接写进 .env、脚本、日志或 commit message。
# - 每次克隆仓库后，只需复制本模板并粘贴自己的 key 即可。

PASTE_YOUR_API_KEY_HERE
